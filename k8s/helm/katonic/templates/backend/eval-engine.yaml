apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.evalEngine.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.evalEngine.name }}
spec:
  replicas: {{ .Values.evalEngine.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.evalEngine.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.evalEngine.name }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: {{ .Values.evalEngine.name }}
          image: {{ .Values.imageRegistry }}{{ .Values.evalEngine.image.repository }}:{{ .Values.evalEngine.image.tag }}
          imagePullPolicy: {{ .Values.evalEngine.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.evalEngine.containerPort }}
          envFrom:
            - configMapRef:
                name: {{ .Values.global.configMap }}
            - secretRef:
                name: {{ .Values.global.secret }}
          resources:
            {{- toYaml .Values.evalEngine.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.evalEngine.containerPort }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.evalEngine.containerPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.evalEngine.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.evalEngine.name }}
spec:
  selector:
    app: {{ .Values.evalEngine.name }}
  ports:
    - port: {{ .Values.evalEngine.service.port }}
      targetPort: {{ .Values.evalEngine.service.targetPort }}
      protocol: TCP


