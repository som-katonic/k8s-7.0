apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.agentApi.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.agentApi.name }}
spec:
  replicas: {{ .Values.agentApi.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.agentApi.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.agentApi.name }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: {{ .Values.agentApi.name }}
          image: {{ .Values.imageRegistry }}{{ .Values.agentApi.image.repository }}:{{ .Values.agentApi.image.tag }}
          imagePullPolicy: {{ .Values.agentApi.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.agentApi.containerPort }}
          envFrom:
            - configMapRef:
                name: {{ .Values.global.configMap }}
            - secretRef:
                name: {{ .Values.global.secret }}
          resources:
            {{- toYaml .Values.agentApi.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.agentApi.containerPort }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.agentApi.containerPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.agentApi.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.agentApi.name }}
spec:
  selector:
    app: {{ .Values.agentApi.name }}
  ports:
    - port: {{ .Values.agentApi.service.port }}
      targetPort: {{ .Values.agentApi.service.targetPort }}
      protocol: TCP


