---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.agentApi.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.agentApi.name }}
spec:
  selector:
    app: {{ .Values.agentApi.name }}
  ports:
    - name: http-api
      port: {{ .Values.agentApi.service.port }}
      targetPort: {{ .Values.agentApi.service.targetPort }}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.agentApi.name }}
  namespace: {{ .Values.global.namespace }}
spec:
  replicas: {{ .Values.agentApi.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.agentApi.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.agentApi.name }}
    spec:
      enableServiceLinks: false

      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}

      containers:
        - name: {{ .Values.agentApi.name }}

          image: "{{ .Values.agentApi.image.repository }}:{{ .Values.agentApi.image.tag }}"
          imagePullPolicy: {{ .Values.agentApi.image.pullPolicy }}

          ports:
            - containerPort: {{ .Values.agentApi.containerPort }}

          env:
            - name: PLATFORM_ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.global.configMap }}
                  key: PLATFORM_ENVIRONMENT

            - name: PLATFORM_SERVICE_NAME
              value: "{{ .Values.agentApi.name }}"

            - name: POSTGRES_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.secret }}
                  key: POSTGRES_URL

            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.secret }}
                  key: REDIS_URL

            - name: AGENT_RUNTIME_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.global.configMap }}
                  key: AGENT_RUNTIME_URL

            - name: AI_GATEWAY_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.global.configMap }}
                  key: AI_GATEWAY_URL

            - name: OTEL_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.global.configMap }}
                  key: OTEL_ENABLED

            # Keycloak
            - name: KEYCLOAK_SERVER_URL
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.global.configMap }}
                  key: KEYCLOAK_SERVER_URL
            - name: KEYCLOAK_REALM
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.global.configMap }}
                  key: KEYCLOAK_REALM
            - name: KEYCLOAK_AUDIENCE
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.global.configMap }}
                  key: KEYCLOAK_AUDIENCE
            - name: KEYCLOAK_ISSUER
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.global.configMap }}
                  key: KEYCLOAK_ISSUER

          resources:
            requests:
              cpu: {{ .Values.agentApi.resources.requests.cpu }}
              memory: {{ .Values.agentApi.resources.requests.memory }}
            limits:
              cpu: {{ .Values.agentApi.resources.limits.cpu }}
              memory: {{ .Values.agentApi.resources.limits.memory }}

          startupProbe:
            httpGet:
              path: /v1/health
              port: {{ .Values.agentApi.containerPort }}
            failureThreshold: 30
            periodSeconds: 10
            timeoutSeconds: 3

          readinessProbe:
            httpGet:
              path: /v1/health
              port: {{ .Values.agentApi.containerPort }}
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /v1/health
              port: {{ .Values.agentApi.containerPort }}
            periodSeconds: 15
            failureThreshold: 5
