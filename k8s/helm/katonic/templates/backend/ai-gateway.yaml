apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.aiGateway.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.aiGateway.name }}
spec:
  replicas: {{ .Values.aiGateway.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.aiGateway.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.aiGateway.name }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: {{ .Values.aiGateway.name }}
          image: {{ .Values.imageRegistry }}{{ .Values.aiGateway.image.repository }}:{{ .Values.aiGateway.image.tag }}
          imagePullPolicy: {{ .Values.aiGateway.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.aiGateway.containerPort }}
          envFrom:
            - configMapRef:
                name: {{ .Values.global.configMap }}
            - secretRef:
                name: {{ .Values.global.secret }}
          resources:
            {{- toYaml .Values.aiGateway.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.aiGateway.containerPort }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.aiGateway.containerPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.aiGateway.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.aiGateway.name }}
spec:
  selector:
    app: {{ .Values.aiGateway.name }}
  ports:
    - port: {{ .Values.aiGateway.service.port }}
      targetPort: {{ .Values.aiGateway.service.targetPort }}
      protocol: TCP


