apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.adminApi.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.adminApi.name }}
spec:
  replicas: {{ .Values.adminApi.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.adminApi.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.adminApi.name }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: {{ .Values.adminApi.name }}
          image: {{ .Values.imageRegistry }}{{ .Values.adminApi.image.repository }}:{{ .Values.adminApi.image.tag }}
          imagePullPolicy: {{ .Values.adminApi.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.adminApi.containerPort }}
          envFrom:
            - configMapRef:
                name: {{ .Values.global.configMap }}
            - secretRef:
                name: {{ .Values.global.secret }}
          resources:
            {{- toYaml .Values.adminApi.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.adminApi.containerPort }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.adminApi.containerPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.adminApi.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.adminApi.name }}
spec:
  selector:
    app: {{ .Values.adminApi.name }}
  ports:
    - port: {{ .Values.adminApi.service.port }}
      targetPort: {{ .Values.adminApi.service.targetPort }}
      protocol: TCP


