apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.mcpGateway.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.mcpGateway.name }}
spec:
  replicas: {{ .Values.mcpGateway.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.mcpGateway.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.mcpGateway.name }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: {{ .Values.mcpGateway.name }}
          image: {{ .Values.imageRegistry }}{{ .Values.mcpGateway.image.repository }}:{{ .Values.mcpGateway.image.tag }}
          imagePullPolicy: {{ .Values.mcpGateway.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.mcpGateway.containerPort }}
          env:
            - name: MCP_TIMEOUT
              value: {{ .Values.mcpGateway.timeout | quote }}
          envFrom:
            - configMapRef:
                name: {{ .Values.global.configMap }}
            - secretRef:
                name: {{ .Values.global.secret }}
          resources:
            {{- toYaml .Values.mcpGateway.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.mcpGateway.containerPort }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.mcpGateway.containerPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mcpGateway.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.mcpGateway.name }}
spec:
  selector:
    app: {{ .Values.mcpGateway.name }}
  ports:
    - port: {{ .Values.mcpGateway.service.port }}
      targetPort: {{ .Values.mcpGateway.service.targetPort }}
      protocol: TCP


