apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.governanceProxy.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.governanceProxy.name }}
spec:
  replicas: {{ .Values.governanceProxy.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.governanceProxy.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.governanceProxy.name }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: {{ .Values.governanceProxy.name }}
          image: {{ .Values.imageRegistry }}{{ .Values.governanceProxy.image.repository }}:{{ .Values.governanceProxy.image.tag }}
          imagePullPolicy: {{ .Values.governanceProxy.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.governanceProxy.containerPort }}
          envFrom:
            - configMapRef:
                name: {{ .Values.global.configMap }}
            - secretRef:
                name: {{ .Values.global.secret }}
          resources:
            {{- toYaml .Values.governanceProxy.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.governanceProxy.containerPort }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.governanceProxy.containerPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.governanceProxy.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.governanceProxy.name }}
spec:
  selector:
    app: {{ .Values.governanceProxy.name }}
  ports:
    - port: {{ .Values.governanceProxy.service.port }}
      targetPort: {{ .Values.governanceProxy.service.targetPort }}
      protocol: TCP


