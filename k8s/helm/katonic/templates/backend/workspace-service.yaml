apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.workspaceService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.workspaceService.name }}
spec:
  replicas: {{ .Values.workspaceService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.workspaceService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.workspaceService.name }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: {{ .Values.workspaceService.name }}
          image: {{ .Values.imageRegistry }}{{ .Values.workspaceService.image.repository }}:{{ .Values.workspaceService.image.tag }}
          imagePullPolicy: {{ .Values.workspaceService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.workspaceService.service.targetPort }}
          env:
            - name: ORG_NAME
              value: {{ .Values.workspaceService.env.orgName | quote }}
          envFrom:
            - configMapRef:
                name: {{ .Values.global.configMap }}
            - secretRef:
                name: {{ .Values.global.secret }}
          resources:
            {{- toYaml .Values.workspaceService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.workspaceService.service.targetPort }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.workspaceService.service.targetPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.workspaceService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.workspaceService.name }}
spec:
  selector:
    app: {{ .Values.workspaceService.name }}
  ports:
    - port: {{ .Values.workspaceService.service.port }}
      targetPort: {{ .Values.workspaceService.service.targetPort }}
      protocol: TCP


