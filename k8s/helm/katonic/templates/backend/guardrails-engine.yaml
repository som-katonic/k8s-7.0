apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.guardrailsEngine.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.guardrailsEngine.name }}
spec:
  replicas: {{ .Values.guardrailsEngine.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.guardrailsEngine.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.guardrailsEngine.name }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: {{ .Values.guardrailsEngine.name }}
          image: {{ .Values.imageRegistry }}{{ .Values.guardrailsEngine.image.repository }}:{{ .Values.guardrailsEngine.image.tag }}
          imagePullPolicy: {{ .Values.guardrailsEngine.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.guardrailsEngine.containerPort }}
          envFrom:
            - configMapRef:
                name: {{ .Values.global.configMap }}
            - secretRef:
                name: {{ .Values.global.secret }}
          resources:
            {{- toYaml .Values.guardrailsEngine.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.guardrailsEngine.containerPort }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.guardrailsEngine.containerPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.guardrailsEngine.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.guardrailsEngine.name }}
spec:
  selector:
    app: {{ .Values.guardrailsEngine.name }}
  ports:
    - port: {{ .Values.guardrailsEngine.service.port }}
      targetPort: {{ .Values.guardrailsEngine.service.targetPort }}
      protocol: TCP


