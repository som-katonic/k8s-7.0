apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.platformBackend.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.platformBackend.name }}
spec:
  replicas: {{ .Values.platformBackend.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.platformBackend.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.platformBackend.name }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: {{ .Values.platformBackend.name }}
          image: {{ .Values.imageRegistry }}{{ .Values.platformBackend.image.repository }}:{{ .Values.platformBackend.image.tag }}
          imagePullPolicy: {{ .Values.platformBackend.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.platformBackend.service.targetPort }}
          envFrom:
            - configMapRef:
                name: {{ .Values.global.configMap }}
            - secretRef:
                name: {{ .Values.global.secret }}
          resources:
            {{- toYaml .Values.platformBackend.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.platformBackend.service.targetPort }}
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.platformBackend.service.targetPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.platformBackend.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.platformBackend.name }}
spec:
  selector:
    app: {{ .Values.platformBackend.name }}
  ports:
    - port: {{ .Values.platformBackend.service.port }}
      targetPort: {{ .Values.platformBackend.service.targetPort }}
      protocol: TCP


