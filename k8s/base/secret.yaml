apiVersion: v1
kind: Secret
metadata:
  name: platform-secret
  namespace: katonic
type: Opaque
stringData:
  # ── PostgreSQL ──
  POSTGRES_USER: "platform"
  POSTGRES_PASSWORD: "platform"
  POSTGRES_URL: "postgresql+asyncpg://platform:platform@postgres:5432/platform"
  DATABASE_URL: "postgresql://platform:platform@postgres:5432/platform"
  KE_POSTGRES_DSN: "postgresql+asyncpg://platform:platform@postgres:5432/knowledge_engine"

  # ── Redis ──
  REDIS_URL: "redis://redis:6379/0"
  KE_REDIS_URL: "redis://redis:6379/1"

  # ── ClickHouse ──
  CLICKHOUSE_USER: "default"
  CLICKHOUSE_PASSWORD: ""

  # ── Encryption Keys ──
  GATEWAY_FERNET_KEY: "yoLDnSQEeP_-DC9kW4Cs4AHeXuqNLFsq-e6EFGdiNdA="
  JWT_SECRET: "change-me-in-production"
  RC_ENCRYPTION_KEY: "change-me-generate-with-cryptography-fernet"

  # ── Keycloak ──
  KEYCLOAK_ADMIN_USER: "admin"
  KEYCLOAK_ADMIN_PASSWORD: "K@ton1c-Adm1n-2026!"
  KEYCLOAK_CLIENT_SECRET: "katonic-platform-api-secret-2026"

  # ── MinIO ──
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "minioadmin"
